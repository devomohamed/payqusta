# 🎨 Animated Notification System - دليل الاستخدام

## نظرة عامة

نظام إشعارات متحرك وجميل يحل محل جميع الـ `alert()` و `toast` في المشروع.

## المزايا

✅ **رسوم متحركة سلسة** - باستخدام Framer Motion
✅ **دعم الوضع الداكن** - يتكيف تلقائياً مع theme
✅ **4 أنواع** - Success, Error, Warning, Info
✅ **قابل للتخصيص** - Duration, Action Buttons, Custom Messages
✅ **سهل الاستخدام** - API بسيط وواضح

## التثبيت

تم إضافة المكون تلقائياً في `App.jsx` - لا حاجة لأي إعداد إضافي!

## الاستخدام الأساسي

### 1. Import

```javascript
import { notify } from '../components/AnimatedNotification';
```

### 2. استخدام بسيط

```javascript
// نجاح
notify.success('تم حفظ البيانات بنجاح');

// خطأ
notify.error('فشلت العملية');

// تحذير
notify.warning('يرجى التحقق من البيانات');

// معلومة
notify.info('هذه رسالة إعلامية');
```

### 3. تخصيص العنوان والرسالة

```javascript
notify.success('تم إنشاء الفاتورة رقم #1234', 'نجحت العملية');

notify.error(
  'لا يوجد مخزون كافٍ من هذا المنتج',
  'خطأ في المخزون'
);
```

### 4. إضافة زر إجراء (Action Button)

```javascript
notify.custom({
  type: 'warning',
  title: 'تحذير: مخزون منخفض',
  message: 'المنتج "قميص أزرق" أوشك على النفاد',
  duration: 8000, // 8 ثواني
  action: {
    label: 'عرض المنتج',
    onClick: () => {
      navigate('/products/123');
    },
  },
});
```

### 5. إشعار دائم (لا يختفي تلقائياً)

```javascript
notify.custom({
  type: 'error',
  title: 'خطأ حرج',
  message: 'فشل الاتصال بالخادم',
  duration: 0, // لا يختفي تلقائياً
});
```

## أمثلة عملية

### مثال 1: عند حفظ بيانات

```javascript
const handleSave = async () => {
  try {
    await api.post('/products', productData);
    notify.success('تم إضافة المنتج بنجاح', 'تم الحفظ');
  } catch (err) {
    notify.error(
      err.response?.data?.message || 'فشل حفظ المنتج',
      'حدث خطأ'
    );
  }
};
```

### مثال 2: تأكيد حذف

```javascript
const handleDelete = async (id) => {
  notify.custom({
    type: 'warning',
    title: 'هل أنت متأكد؟',
    message: 'سيتم حذف هذا العنصر نهائياً',
    duration: 10000,
    action: {
      label: 'تأكيد الحذف',
      onClick: async () => {
        try {
          await api.delete(`/products/${id}`);
          notify.success('تم الحذف بنجاح');
        } catch (err) {
          notify.error('فشل الحذف');
        }
      },
    },
  });
};
```

### مثال 3: عمليات طويلة

```javascript
const handleExport = async () => {
  // بدء العملية
  const notifId = notify.info('جاري تصدير البيانات...', 'جاري المعالجة');

  try {
    const data = await api.get('/export');
    // عند النجاح
    notify.success('تم تصدير 1,250 سجل بنجاح', 'اكتمل التصدير');
  } catch (err) {
    notify.error('فشل التصدير', 'حدث خطأ');
  }
};
```

### مثال 4: استبدال Alerts

```javascript
// ❌ القديم
alert('تم حفظ البيانات');

// ✅ الجديد
notify.success('تم حفظ البيانات بنجاح');
```

```javascript
// ❌ القديم
if (confirm('هل تريد المتابعة؟')) {
  // ...
}

// ✅ الجديد
notify.custom({
  type: 'warning',
  title: 'هل تريد المتابعة؟',
  message: 'هذا الإجراء لا يمكن التراجع عنه',
  action: {
    label: 'نعم، تابع',
    onClick: () => {
      // تنفيذ الإجراء
    },
  },
});
```

## API Reference

### `notify.success(message, title?)`
عرض إشعار نجاح (أخضر)

### `notify.error(message, title?)`
عرض إشعار خطأ (أحمر)

### `notify.warning(message, title?)`
عرض إشعار تحذير (أصفر)

### `notify.info(message, title?)`
عرض إشعار معلومات (أزرق)

### `notify.custom(config)`
إنشاء إشعار مخصص بالكامل

**Config Options:**
```typescript
{
  type: 'success' | 'error' | 'warning' | 'info',
  title: string,
  message: string,
  duration?: number, // بالميلي ثانية (0 = دائم)
  action?: {
    label: string,
    onClick: () => void
  }
}
```

## استبدال Toast و Alert

### استبدال react-hot-toast

```javascript
// ❌ القديم
import toast from 'react-hot-toast';
toast.success('نجح');
toast.error('فشل');
toast.loading('جاري التحميل...');

// ✅ الجديد
import { notify } from '../components/AnimatedNotification';
notify.success('نجح');
notify.error('فشل');
notify.info('جاري التحميل...');
```

### استبدال window.alert()

```javascript
// ❌ القديم
window.alert('تمت العملية بنجاح');

// ✅ الجديد
notify.success('تمت العملية بنجاح');
```

### استبدال window.confirm()

```javascript
// ❌ القديم
if (window.confirm('هل أنت متأكد؟')) {
  deleteItem();
}

// ✅ الجديد
notify.custom({
  type: 'warning',
  title: 'هل أنت متأكد؟',
  message: 'سيتم حذف هذا العنصر',
  action: {
    label: 'تأكيد',
    onClick: deleteItem,
  },
});
```

## الملفات ذات الصلة

- **Component:** `client/src/components/AnimatedNotification.jsx`
- **Integration:** `client/src/App.jsx`
- **Usage Example:** `client/src/pages/BusinessReportsPage.jsx`

## ملاحظات

- ✅ يدعم الوضع الداكن تلقائياً
- ✅ يظهر في أعلى يسار الشاشة (مناسب للعربية)
- ✅ يمكن عرض عدة إشعارات في نفس الوقت
- ✅ شريط تقدم يعرض الوقت المتبقي
- ✅ رسوم متحركة سلسة عند الظهور والاختفاء
- ✅ يمكن إغلاقه يدوياً بزر X
- ✅ يختفي تلقائياً بعد Duration المحدد

## نصائح الاستخدام

1. **للعمليات السريعة**: استخدم duration قصير (3-4 ثواني)
2. **للأخطاء المهمة**: استخدم duration أطول (8-10 ثواني) أو 0 (دائم)
3. **للتأكيدات**: أضف action button مع duration طويل
4. **للتحديثات الحية**: استخدم notify.info مع messages متتالية

---

**📝 ملاحظة:** هذا النظام يحل محل جميع `alert()` و `confirm()` و `toast` في المشروع!
